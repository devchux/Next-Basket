import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#23A6F0",
    },
    secondary: {
      main: "#23856D",
    },
  },
  typography: {
    fontFamily: "Montserrat, sans-serif",
  },
});
